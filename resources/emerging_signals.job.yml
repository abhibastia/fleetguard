resources:
  jobs:
    emerging_signals:
      name: fleetguard-emerging-signals
      tasks:
        - task_key: signals
          disabled: false
          email_notifications: {}
          notebook_task:
            notebook_path: ../src/backtest/10_emerging_signals.py
            source: WORKSPACE
          run_if: ALL_SUCCESS
          timeout_seconds: 3600
      email_notifications: {}
      max_concurrent_runs: 1
      queue:
        enabled: true
      timeout_seconds: 0
      webhook_notifications: {}
